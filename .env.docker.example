# Environment variables for Docker Compose setup

# LLM API Keys (REQUIRED - at least one)
# Get your keys from:
# - Anthropic: https://console.anthropic.com/
# - OpenAI: https://platform.openai.com/api-keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Configure which model to use for generating agent suggestions
SUGGESTER_PROVIDER=anthropic
SUGGESTER_MODEL=claude-haiku-4-5-20251001

# Database Configuration (configured by docker-compose.yml)
# These values match the PostgreSQL container settings
# You typically don't need to change these for Docker
DATABASE_HOST=db
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=debaite_dev

# Phoenix Configuration
PHX_SERVER=true
PHX_HOST=localhost
PORT=4000
MIX_ENV=dev
