# LLM API Keys
# At least one of these is required for the application to work
ANTHROPIC_API_KEY=your_anthropic_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Configure which model to use for generating agent suggestions
# Defaults to Anthropic's Claude Haiku 4.5
SUGGESTER_PROVIDER=anthropic
SUGGESTER_MODEL=claude-haiku-4-5-20251001

# Database configuration (already configured in dev.exs for development)
# DATABASE_URL=ecto://postgres:postgres@localhost/debaite_dev

# Phoenix configuration (optional for development)
# SECRET_KEY_BASE=generate_with_mix_phx_gen_secret
# PHX_HOST=localhost
# PORT=4000
